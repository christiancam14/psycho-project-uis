import { ConvertirStringPipe } from './convertir-string.pipe';

describe('ConvertirStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirStringPipe();
    expect(pipe).toBeTruthy();
  });
});
